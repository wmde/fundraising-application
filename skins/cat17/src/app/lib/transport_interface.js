/**
 * @interface Transport
 */

/**
 * @function
 * @name Transport#getData
 * @param {string} uri URI end point
 * @param {object} requestData key => value pairs to send (via URL parameters)
 * @return {Promise}
 */

/**
 * @function
 * @name Transport#postData
 * @param {string} uri URI end point
 * @param {object} requestData key => value pairs to send (via POST parameters)
 * @return {Promise}
 */