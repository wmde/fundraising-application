{% spaceless %}
{% for amount in presetAmounts %}
	<div class="wrap-radio">
		{# TODO use proper money objects for predefined amounts in templates #}
		<input type="radio" id="amount{$ loop.index $}" value="{$ amount.getEuroCents $}">
		<label for="amount{$ loop.index $}">
			<span>{$ amount.getEuroFloat() | number_format( 0 ) $} €</span>
		</label>
	</div>
{% endfor %}
{% endspaceless %}