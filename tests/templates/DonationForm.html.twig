Amount: {$ initialFormValues.amount $}
Payment type: {$ initialFormValues.paymentType $}
Interval: {$ initialFormValues.paymentIntervalInMonths $}
IBAN: {$ initialFormValues.iban $}
BIC: {$ initialFormValues.bic $}
Bank name: {$ initialFormValues.bankName $}
Address type: {$ initialFormValues.addressType $}
Salutation: {$ initialFormValues.salutation $}
Title: {$ initialFormValues.title $}
Company: {$ initialFormValues.company $}
First name: {$ initialFormValues.firstName $}
Last name: {$ initialFormValues.lastName $}
Street: {$ initialFormValues.street $}
Postal code: {$ initialFormValues.postcode $}
City: {$ initialFormValues.city $}
Country code: {$ initialFormValues.country $}
Email address: {$ initialFormValues.email $}

Violations:
{% for fieldName, message in violatedFields %}
	Value of field "{$ fieldName $}" violates rule: {$ message $}
{% endfor %}
