{% set formattedAmount = membershipFee|localizedcurrency('EUR', 'de_DE') %}
{% if membershipType == 'sustaining' %}
{$ greeting_generator.createGreeting( lastName, salutation, title ) $}
{% else %}
{$ sandboxed_text('membership_application_confirmation/active_membership_salutation') $}
{% endif %}

{$ sandboxed_text('membership_application_confirmation/body', {
    'membership_type': membershipType|trans( {}, 'membershipTypes' ),
    'payment_interval_in_months': paymentIntervalInMonths|trans( {}, 'paymentIntervals' ),
    'formatted_amount': formattedAmount
}) $}

{$ sandboxed_text('name_head_of_organization') $}
{$ sandboxed_text('title_head_of_organization') $}

---------------------------------------------------------------------------
{$ sandboxed_text('wikimedia_vision') $}
https://www.wikimedia.de/
---------------------------------------------------------------------------

{$ sandboxed_text('address_of_organization') $}

{$ sandboxed_text('phone') $}
www.wikimedia.de

{$ sandboxed_text('tax_id_organization') $}
