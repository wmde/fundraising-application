{% set formattedAmount = membershipFee|localizedcurrency('EUR', 'de_DE') %}
{% if membershipType == 'sustaining' %}
{$ greeting_generator.createGreeting( lastName, salutation, title ) $}
{% else %}
{$ sandboxed_content('plaintext/mail_membership_application_confirmation/active_membership_salutation') $}
{% endif %}

{$ sandboxed_content('plaintext/mail_membership_application_confirmation/body', {
    'membership_type': membershipType|trans( {}, 'membershipTypes' ),
    'payment_interval_in_months': paymentIntervalInMonths|trans( {}, 'paymentIntervals' ),
    'formatted_amount': formattedAmount
}) $}

{$ sandboxed_content('plaintext/name_head_of_organization') $}
{$ sandboxed_content('plaintext/title_head_of_organization') $}

---------------------------------------------------------------------------
{$ sandboxed_content('plaintext/wikimedia_vision') $}
https://www.wikimedia.de/
---------------------------------------------------------------------------

{$ sandboxed_content('plaintext/address_of_organization') $}

{$ sandboxed_content('plaintext/phone') $}
www.wikimedia.de

{$ sandboxed_content('plaintext/tax_id_organization') $}
