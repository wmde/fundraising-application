{$ greeting_generator.createGreeting( subscription.address.lastName, subscription.address.salutation, subscription.address.title ) $}

{$ sandboxed_text('subscription_request', {
    'confirm_subscription_link': url( 'confirm-subscription', { confirmationCode: subscription.confirmationCode } )
}) $}

{$ sandboxed_text('name_head_of_suborganization') $}
{$ sandboxed_text('title_head_of_suborganization') $}

{$ sandboxed_text('donation_hint') $}

-------------------------------------
{$ sandboxed_text('address_of_suborganization') $}
{$ sandboxed_text('phone') $}
{$ sandboxed_text('email') $}
{$ sandboxed_text('bank_account') $}

{$ sandboxed_text('tax_id_suborganization') $}

{$ sandboxed_text('unsubscribe_hint', {
    'subscription_email': subscription.email,
    'privacy_protection_link': url( 'page', { pageName: 'Datenschutz' } )
}) $}
